type Query {
    categories(limit: Int, offset: Offset, parent: String, title: String, sort: Sort ): [Category]
    allCategories(limit: Int, offset: Offset, parent: String, title: String, sort:  Sort  ): [Category]
}

type Mutation {
    newCategory(title: String!, parent: ID): Boolean
}
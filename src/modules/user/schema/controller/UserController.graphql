type Query {
    users(limit: Int, offset: Int): [User]
    user(id: ID): User
    me: User
}

type Mutation {
    userSignUp(mobile: String!, firstName: String, lastName: String): Boolean
    userSignIn(mobile: String!): Boolean
    userVerify(mobile: String!,code: String!): User
}